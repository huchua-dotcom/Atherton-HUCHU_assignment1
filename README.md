# Atherton Huchu — Profile / Mini-Portfolio Page

A one-page, semantic HTML5 profile and mini-portfolio built for Practical Assignment 1
(Unit II, Web Technologies — CIS2103, Africa University). Will be extended with
responsive CSS3 and Tailwind styling in the Unit III assignment.

## AI-Assisted Content

My About-section prompt, the AI's raw output, my edited final version, and my
reflection on the changes are in [`PROMPT_LOG.md`](./PROMPT_LOG.md).

## Accessibility: Peer Review Issue & Fix

> ⚠️ Replace this section with the *actual* issue your peer reviewer found on
> Thursday and the fix you actually made. This is evidence the feedback loop
> happened — don't fabricate it.

**Issue found by peer reviewer:** [e.g. "My phone number input had no associated
label, so a screen reader announced it only as 'edit text'."]

**Fix applied:** [e.g. "Added a `<label for="phone">Phone number</label>` tied to
the input's `id`, and confirmed with the browser's accessibility inspector that
the field is now announced correctly."]

## Running Locally

Clone the repository and open `index.html` directly in a browser — no build step
required.

```
git clone https://github.com/YOUR-USERNAME/atherton-huchu-profile.git
cd atherton-huchu-profile
open index.html   # or double-click the file
```

## Structure

```
atherton-huchu-profile/
├── index.html      # Semantic HTML5 page + contact form
├── style.css       # Base styling and accessible contrast
├── README.md        # This file
└── PROMPT_LOG.md     # Part C: AI prompt, raw output, edit, reflection
```
